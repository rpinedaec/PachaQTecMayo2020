#Para crear este modelo; se debe ejecutar en la línea de comando: orator make:model User
# Orator, reconoce todo como letras minusculas, por lo que se crea el archivo "user.py"
from orator import Model


class User(Model):

    pass
